rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir lectura y escritura a las colecciones de la aplicación INDOMABLE CrossFit
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
